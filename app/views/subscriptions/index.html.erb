<%= turbo_frame_tag "subscriptions" do %>
  <h1>Subscriptions</h1>
  <turbo-frame id="new_subscription">
      <%= form_with(url: subscriptions_path,
              data: { turbo: true }) do |f| %>
      <%= f.text_field :feed_url, 
          placeholder: "Enter link",
          required: true %>
      <%= f.submit "Add subscription",
          data: { disable_with: "Adding..." } %>
    <% end %>
  </turbo-frame>
  <turbo-frame id="subscription_list">
    <% if @subscriptions.present? %>
      <%= render partial: 'subscriptions/preview', collection: @subscriptions, cached: true %>
    <% else %>
      <p>No subscriptions found.</p>
    <% end %>
  </turbo-frame>
<% end %>